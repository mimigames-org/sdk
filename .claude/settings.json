{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo \"$CLAUDE_TOOL_INPUT\" | grep -q -- '--no-verify'; then echo 'BLOCKED: --no-verify is not allowed. Fix the underlying issue instead of bypassing hooks.' >&2; exit 2; fi"
          }
        ]
      }
    ]
  }
}
