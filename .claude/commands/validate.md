Validate the SDK: run lint and tests.

1. Run `npm install` if node_modules is missing
2. Run `npm run lint` to check for code issues
3. Run `npm test` to run the test suite
4. Report any issues found
